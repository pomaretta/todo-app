// Database credentials
const USER = ""; // Username
const PASSWORD = ""; // Password
const HOSTNAME = ""; // Host address EG: 0.0.0.0, example.com
const PORT = null; // Custom port for address
const DATABASE = ""; // Database name

// Keys object
const database = {
    host: HOSTNAME,
    db: DATABASE,
    port: PORT,
    user: USER,
    password: PASSWORD
}

module.exports = database;